# 9)კომენტარის სახით ჩამოწერეთ სიის ყველა ფუნქცია და მიუწერეთ თ რომელი რას აკეთებს



# append() - სიის ბოლოში ამატებს ელემენტს
# insert() - ამატებს ელემენტს მითითებულ ინდექსზე
# remove() - შლის პირველივე ელემენტს სიიდან
# pop() - შლის ელემენტს (თუ i არ არის, შლის ბოლოს)
# clear() - მთლიანად ასუფთავებს სიას
# index() - აბრუნებს ელემენტის ინდექსს
# count() - ითვლის რამდენჯერ გვხვდება ელემენტი სიაში
# len() - ითვლის სიის სიგრძეს (ელემენტების რაოდენობას)
# sum() - ითვლის სიის ელემენტების ჯამს
